<?php
    // auteur: junior 
    // fucntie: Print kleur van het stop licht
    // print rood als kleurcode = 1
    // print groen als kleurcode = 2
    // print oranje als kleurcode = 3

    // initialisatie
    $kleur = 1; // rood

    //test of de kleur rood is
    if ($kleur == 1) {
        // print kleur
        echo "stop licht staat op rood";
    }

    // initialisatie
    $kleur = 2; // groen

    //test of de kleur groen is
    if ($kleur == 2) {
        // print kleur
        echo "stop licht staat op groen";
    }

    // initialisatie
    $kleur = 3; // oranje

    //test of de kleur oranje is
    if ($kleur == 3) {
        // print kleur
        echo "stop licht staat op oranje";
     } elseif ($kleur == 3) {// test oranje
            echo "stoplicht staat op oranje";

    }
    else {
        echo "stoplicht knippert oranje";
    }

?> 
